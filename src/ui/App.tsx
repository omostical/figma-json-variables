import React from "react";

export default function App() {
  return (
    <div className="flex flex-col h-full min-h-screen p-4 bg-white font-sans">
      <h1 className="text-lg font-semibold text-gray-900">JSON Variables Importer</h1>
      <p className="text-sm text-gray-500 mt-1">
        Full UI coming in M4 — core pipeline (M2) is ready.
      </p>
    </div>
  );
}
